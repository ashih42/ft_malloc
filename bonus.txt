- Debug environment variables
	MALLOC_VERBOSE=1 - logs history of calls to malloc(), free(), calloc(), realloc()
	MALLOC_VISUAL=1 - activates VISUALIZER

- show_alloc_mem_ex() shows hexdump of all allocated bytes

- Defragment memory - to prevent memory fragmentation after free()

- Checksum to validate block metadata; safely abort() program if corruption detected

- Thread-safety using mutex

- Multi-threading with VISUALIZER

- VISUALIZER using OpenGL

- WONDERFUL diagram

